Functional Doctest
==================

:Test-Layer: functional

   >>> import grok
   >>> from uvcsite.app import Uvcsite  # This is a grok.Application
   >>> root = getRootFolder()
   >>> uvcsite = Uvcsite()
   >>> root['app'] = uvcsite

   >>> class MyContainer(grok.Model):
   ...    """ MyContainer"""

   >>> mc = MyContainer()

   >>> #mc = grok.Container()

   >>> root['app']['mc'] = mc

   >>> import transaction
   >>> transaction.commit()
